---
title: 텍스트 필드
label: 텍스트
order: 180
desc: 텍스트 필드 타입은 단순히 문자열을 데이터베이스에 저장하고 관리 패널에 텍스트 입력을 제공합니다. 텍스트 필드 사용 방법, 예제 및 옵션을 확인하세요.
keywords: text, fields, config, configuration, documentation, Content Management System, cms, headless, javascript, node, react, nextjs
---

텍스트 필드는 가장 일반적으로 사용되는 필드 중 하나입니다. 문자열을 데이터베이스에 저장하고 [관리 패널](../admin/overview)에 간단한 텍스트 입력을 제공합니다.

<LightDarkImage
  srcLight="https://payloadcms.com/images/docs/fields/text.png"
  srcDark="https://payloadcms.com/images/docs/fields/text-dark.png"
  alt="Payload 관리 패널의 텍스트 필드와 읽기 전용 텍스트 필드를 보여줍니다"
  caption="텍스트 필드와 읽기 전용 텍스트 필드의 관리 패널 스크린샷"
/>

텍스트 필드를 추가하려면 [필드 구성](./overview)에서 `type`을 `text`로 설정하세요:

```ts
import type { Field } from 'payload'

export const MyTextField: Field = {
  // ...
  type: 'text', // highlight-line
}
```

## 구성 옵션

| 옵션                   | 설명                                                                                                                                                                                                                                                                                                    |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`name`** \*          | 데이터베이스에 저장되고 검색될 때 속성 이름으로 사용됩니다. [자세한 정보](/docs/fields/overview#field-names).                                                                                                                                                                                          |
| **`label`**            | 관리 패널에서 필드 라벨로 사용되는 텍스트 또는 각 언어의 키를 가진 객체입니다.                                                                                                                                                                                                                      |
| **`unique`**           | 컬렉션의 각 항목이 이 필드에 대해 고유한 값을 갖도록 강제합니다.                                                                                                                                                                                                                                   |
| **`minLength`**        | 기본 검증 함수에서 값이 최소 문자 길이를 갖도록 하는 데 사용됩니다.                                                                                                                                                                                                                                |
| **`maxLength`**        | 기본 검증 함수에서 값이 최대 문자 길이를 갖도록 하는 데 사용됩니다.                                                                                                                                                                                                                                |
| **`validate`**         | 관리 패널과 백엔드 모두에서 실행될 사용자 정의 검증 함수를 제공합니다. [자세한 정보](/docs/fields/overview#validation).                                                                                                                                                                           |
| **`index`**            | 더 빠른 쿼리를 생성하기 위해 이 필드에 대한 [인덱스](../database/indexes)를 구축합니다. 사용자가 이 필드의 데이터를 자주 쿼리하는 경우 이 필드를 `true`로 설정하세요.                                                                                                                              |
| **`saveToJWT`**        | 이 필드가 최상위 레벨이고 [인증](/docs/authentication/overview)을 지원하는 구성에 중첩된 경우, 사용자 JWT에 해당 데이터를 포함합니다.                                                                                                                                                             |
| **`hooks`**            | 이 필드에 대한 로직을 제어하기 위해 필드 훅을 제공합니다. [자세한 정보](../hooks/fields).                                                                                                                                                                                                           |
| **`access`**           | 사용자가 이 필드의 데이터로 무엇을 보고 할 수 있는지를 나타내는 필드 접근 제어를 제공합니다. [자세한 정보](../access-control/fields).                                                                                                                                                            |
| **`hidden`**           | 모든 API에서 이 필드의 가시성을 완전히 제한합니다. 여전히 데이터베이스에 저장되지만 어떤 API나 관리 패널에도 나타나지 않습니다.                                                                                                                                                                    |
| **`defaultValue`**     | 이 필드의 기본값으로 사용할 데이터를 제공합니다. [자세한 정보](/docs/fields/overview#default-values).                                                                                                                                                                                              |
| **`localized`**        | 이 필드에 대해 지역화를 활성화합니다. 기본 구성에서 [지역화가 활성화되어야 합니다](/docs/configuration/localization).                                                                                                                                                                            |
| **`required`**         | 이 필드에 값이 있어야 합니다.                                                                                                                                                                                                                                                                      |
| **`admin`**            | 관리자별 구성입니다. [자세한 정보](#admin-options).                                                                                                                                                                                                                                                |
| **`custom`**           | 사용자 정의 데이터(예: 플러그인용)를 추가하기 위한 확장 지점입니다.                                                                                                                                                                                                                               |
| **`hasMany`**          | 이 필드를 단일 텍스트가 아닌 순서가 지정된 텍스트 배열로 만듭니다.                                                                                                                                                                                                                                |
| **`minRows`**          | `hasMany`가 true로 설정된 경우 배열의 최소 텍스트 수입니다.                                                                                                                                                                                                                                       |
| **`maxRows`**          | `hasMany`가 true로 설정된 경우 배열의 최대 텍스트 수입니다.                                                                                                                                                                                                                                       |
| **`typescriptSchema`** | JSON 스키마를 제공하여 필드 타입 생성을 재정의합니다.                                                                                                                                                                                                                                             |
| **`virtual`**          | 데이터베이스에서 필드를 비활성화하려면 `true`를 제공하거나, [필드를 관계와 연결](/docs/fields/relationship#linking-virtual-fields-with-relationships)하려면 문자열 경로를 제공하세요. [가상 필드](https://payloadcms.com/blog/learn-how-virtual-fields-can-help-solve-common-cms-challenges) 참조 |

_\* 별표는 필수 속성을 나타냅니다._

## 관리자 옵션

[관리 패널](../admin/overview)에서 텍스트 필드의 모양과 동작을 사용자 정의하려면 `admin` 옵션을 사용할 수 있습니다:

```ts
import type { Field } from 'payload'

export const MyTextField: Field = {
  // ...
  admin: {
    // highlight-line
    // ...
  },
}
```

텍스트 필드는 기본 [필드 관리자 구성](./overview#admin-options)의 모든 기본 관리자 옵션을 상속받으며, 다음과 같은 추가 옵션들이 있습니다:

| 옵션               | 설명                                                                                                                        |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------- |
| **`placeholder`**  | 텍스트 입력에서 플레이스홀더 문자열을 정의하려면 이 속성을 설정하세요.                                                      |
| **`autoComplete`** | 브라우저 자동완성에 사용될 문자열로 이 속성을 설정하세요.                                                                   |
| **`rtl`**          | 이 필드에 대한 관리 패널의 기본 텍스트 방향을 재정의합니다. 오른쪽에서 왼쪽 텍스트 방향을 강제하려면 `true`로 설정하세요. |

## 예제

`collections/ExampleCollection.ts`

```ts
import type { CollectionConfig } from 'payload'

export const ExampleCollection: CollectionConfig = {
  slug: 'example-collection',
  fields: [
    {
      name: 'pageTitle', // 필수
      type: 'text', // 필수
      required: true,
    },
  ],
}
```

## 사용자 정의 컴포넌트

### 필드

#### 서버 컴포넌트

```tsx
import type React from 'react'
import { TextField } from '@payloadcms/ui'
import type { TextFieldServerComponent } from 'payload'

export const CustomTextFieldServer: TextFieldServerComponent = ({
  clientField,
  path,
  schemaPath,
  permissions,
}) => {
  return (
    <TextField
      field={clientField}
      path={path}
      schemaPath={schemaPath}
      permissions={permissions}
    />
  )
}
```

#### 클라이언트 컴포넌트

```tsx
'use client'
import React from 'react'
import { TextField } from '@payloadcms/ui'
import type { TextFieldClientComponent } from 'payload'

export const CustomTextFieldClient: TextFieldClientComponent = (props) => {
  return <TextField {...props} />
}
```

### 라벨

#### 서버 컴포넌트

```tsx
import React from 'react'
import { FieldLabel } from '@payloadcms/ui'
import type { TextFieldLabelServerComponent } from 'payload'

export const CustomTextFieldLabelServer: TextFieldLabelServerComponent = ({
  clientField,
  path,
  required,
}) => {
  return (
    <FieldLabel
      label={clientField?.label || clientField?.name}
      path={path}
      required={clientField?.required}
    />
  )
}
```

#### 클라이언트 컴포넌트

```tsx
'use client'
import React from 'react'
import { FieldLabel } from '@payloadcms/ui'
import type { TextFieldLabelClientComponent } from 'payload'

export const CustomTextFieldLabelClient: TextFieldLabelClientComponent = ({
  field,
  path,
}) => {
  return (
    <FieldLabel
      label={field?.label || field?.name}
      path={path}
      required={field?.required}
    />
  )
}
```