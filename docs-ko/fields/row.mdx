---
title: 행 필드
label: 행
order: 150
desc: 행 필드를 사용하면 관리자 패널에서 필드를 서로 나란히 배치하여 대시보드를 커스터마이징할 수 있습니다.
keywords: 행, 필드, 구성, 설정, 문서, 콘텐츠 관리 시스템, cms, 헤드리스, javascript, node, react, nextjs
---

행 필드는 프레젠테이션 전용 필드이며 [관리자 패널](../admin/overview)에만 영향을 미칩니다. 이를 사용하여 [필드](./overview)를 수평으로 나란히 배치할 수 있습니다.

<LightDarkImage
  srcLight="https://payloadcms.com/images/docs/fields/row.png"
  srcDark="https://payloadcms.com/images/docs/fields/row-dark.png"
  alt="Payload 관리자 패널의 행 필드를 보여줍니다"
  caption="행 필드의 관리자 패널 스크린샷"
/>

행 필드를 추가하려면 [필드 Config](./overview)에서 `type`을 `row`로 설정하세요:

```ts
import type { Field } from 'payload'

export const MyRowField: Field = {
  // ...
  // highlight-start
  type: 'row',
  fields: [
    // ...
  ],
  // highlight-end
}
```

## 구성 옵션

| 옵션            | 설명                                                                                                                          |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| **`fields`** \* | 이 행 내에 중첩할 필드 타입의 배열.                                                                                           |
| **`admin`**     | `description`, `readOnly`, `hidden`을 제외한 관리자별 구성. [자세한 내용](./overview#admin-options).                       |
| **`custom`**    | 커스텀 데이터 추가를 위한 확장 지점 (예: 플러그인용)                                                                          |

_\* 별표는 필수 속성을 나타냅니다._

## 예제

`collections/ExampleCollection.ts`

```ts
import type { CollectionConfig } from 'payload'

export const ExampleCollection: CollectionConfig = {
  slug: 'example-collection',
  fields: [
    {
      type: 'row', // required
      fields: [
        // required
        {
          name: 'label',
          type: 'text',
          required: true,
          admin: {
            width: '50%',
          },
        },
        {
          name: 'value',
          type: 'text',
          required: true,
          admin: {
            width: '50%',
          },
        },
      ],
    },
  ],
}
```